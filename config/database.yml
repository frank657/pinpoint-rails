# PostgreSQL. Versions 9.5 and up are supported.
#
# Multi-database layout (primary + Solid Queue/Cache/Cable) across all environments —
# see docs/decisions/0007 and docs/ARCHITECTURE.md.
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  primary: &primary_development
    <<: *default
    database: pinpoint_rails_development
  cache:
    <<: *primary_development
    database: pinpoint_rails_development_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_development
    database: pinpoint_rails_development_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_development
    database: pinpoint_rails_development_cable
    migrations_paths: db/cable_migrate

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  primary: &primary_test
    <<: *default
    database: pinpoint_rails_test
  cache:
    <<: *primary_test
    database: pinpoint_rails_test_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_test
    database: pinpoint_rails_test_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_test
    database: pinpoint_rails_test_cable
    migrations_paths: db/cable_migrate

# Production: a single Postgres (provided by Dokku as DATABASE_URL) hosts the primary app
# data and all three Solid stores (Cache / Queue / Cable). Each connects to the SAME database
# via DATABASE_URL; their separate migration paths keep the Solid tables distinct within it.
# To split them onto dedicated databases later, give cache/queue/cable their own *_DATABASE_URLs.
production:
  primary: &primary_production
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
  cache:
    <<: *primary_production
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    migrations_paths: db/cable_migrate
